/// مستوى من مستويات نظام النقاط (XP).
class AppLevel {
  final int level;
  final String titleAr;
  final int xpRequired;

  const AppLevel({
    required this.level,
    required this.titleAr,
    required this.xpRequired,
  });
}
